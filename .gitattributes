# 기본: 텍스트 파일은 LF로 통일
* text=auto eol=lf

# 셸 스크립트는 반드시 LF (Windows에서 CRLF로 변환되면 Docker에서 실행 불가)
*.sh text eol=lf

# Windows 전용 파일은 CRLF 허용
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리 파일
*.png binary
*.jpg binary
*.ico binary
*.pem binary
